using System;
using System.Collections.Generic;
using System.Text;
using Xunit;

namespace WitpBusinessLogicTests
{
    public class WitpUserSessionTests
    {
        [Fact]
        public void SomeTest()
        {
            //var userSession = new WitpUserSession();
            //userSession.Start(); // ?
            //var example = userSession.GetNextExample(); // Based on history selects the proper example (it might be random or something difficult for the user)
            //var response = userSession.CheckTheAnswer(example, answer); // Checks the answer, save result to DB, 
            //userSession.Stop();
            //var statistics = userSession.GetStatics();
        }
    }
}
